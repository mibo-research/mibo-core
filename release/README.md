# Release records

The authoritative frozen scientific release is **MIBO Core Protocol Package v1.0**, archived on Zenodo under DOI `10.5281/zenodo.21936410`.

`PACKAGE_MANIFEST_v1.0-rc3.json` is retained solely as a historical pre-release manifest. It is not the manifest of the current v1.0 scientific release.

For the exact v1.0 archival file list and SHA-256 values, use `SHA256SUMS.txt` inside the Zenodo v1.0 deposit. For post-freeze GitHub software provenance, use Git commit SHAs and CI results.
